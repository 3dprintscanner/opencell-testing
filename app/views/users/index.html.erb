<div class="container text-center">
  <br>

  <p id="notice"><%= notice %></p>

  <h1>Users</h1>

  <table class="table table-striped">
    <thead class="thead-dark">
      <tr>
        <th>Email</th>
        <th colspan="2"></th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.email %></td>
          <td><%= link_to 'Show', user, class: 'btn btn-primary' %></td>
          <td><%= link_to 'Deactivate', user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' unless !user.is_active? %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to '+ New User', new_user_path %>
</div>